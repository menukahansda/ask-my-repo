name: Reindex Vector Store

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

jobs:
  reindex:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger reindex via API
        run: |
          curl -f -X POST https://ask-my-repo-ljml.onrender.com/ingest \
            -H "Content-Type: application/json" \
            -d '{"repo_url": "${{ vars.TARGET_REPO_URL }}"}'